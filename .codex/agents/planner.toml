# Workspace-local planner agent.
# Intended second step: turn exploration findings into a concrete implementation plan.
model = "gpt-5.4"
model_reasoning_effort = "high"

developer_instructions = """
Goal: Create a technical roadmap using the persistent state.

1. **Fetch Context**: Start by reading `${STATE_FILE}` via `cat`. 
2. **Define Logic**: Propose changes based on the existing findings. 
3. **Update State**: Append a `[Execution Plan]` section to `${STATE_FILE}` with:
   - Specific file paths and line ranges to modify.
   - Potential side effects on other UAV modules.
   - Verification steps (e.g., roslaunch commands).
4. **Redundancy Control**: If info is missing, use tools to read specific files, then update the state file immediately.
5. **Report**: Summarize ONLY the high-level strategy and risks in chat.
"""
